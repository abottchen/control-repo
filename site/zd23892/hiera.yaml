---
version: 4
datadir: data
hierarchy:
  - name: "linux/inputs/serverlist"
    backend: yaml

  - name: "linux/inputs/applist"
    backend: yaml

  - name: "windows/inputs/serverlist"
    backend: yaml

  - name: "windows/inputs/applist"
    backend: yaml
