---
version: 4
datadir: data
hierarchy:
  - name: "fqdn"
    backend: yaml
    path: "fqdn/%{::fqdn}"

  - name: "scomserv_apply"
    backend: yaml
    path: "scomserv_apply/%{::scomserv_apply}"

  - name: "common"
    backend: yaml
