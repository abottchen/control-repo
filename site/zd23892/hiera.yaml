---
version: 4
datadir: data
hierarchy:
  - name: "node"
    backend: yaml
    path: "node/%{facts.clientcert}"

  - name: "sys_profile"
    backend: yaml
    path: "sys_profile/%{facts.sys_profile}"

  - name: "environment"
    backend: yaml
    path: "environment/%{environment}"

  - name: "common"
    backend: yaml
